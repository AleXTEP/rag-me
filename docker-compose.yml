services:
  weaviate:
    image: semitechnologies/weaviate:1.24.0
    ports:
      - "8080:8080"
      - "50051:50051"
    environment:
      QUERY_DEFAULTS_LIMIT: 25
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: 'true'
      PERSISTENCE_DATA_PATH: /var/lib/weaviate
      DEFAULT_VECTORIZER_MODULE: 'none'
      ENABLE_MODULES: 'text2vec-openai,text2vec-cohere,text2vec-huggingface'
      CLUSTER_HOSTNAME: 'node1'
    volumes:
      - weaviate_data:/var/lib/weaviate
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/v1/.well-known/ready"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    ports:
      - "9200:9200"
      - "9300:9300"
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s

  api:
    build:
      context: .
    platform: linux/amd64
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379/0
      - WEAVIATE_URL=http://weaviate:8080
      - ELASTICSEARCH_URL=http://elasticsearch:9200
      - EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
      - RERANKER_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2
      - UPLOAD_DIR=/app/uploads
      - CHUNKING_STRATEGY=fixed
      - EXTRACTION_BACKEND=pymupdf
      - HYDE_PROVIDER=claude
      - HYDE_MODEL=claude-haiku-4-5-20251001
      - HYDE_API_KEY=${HYDE_API_KEY:-}
      - HYDE_LOCAL_URL=http://localhost:11434
      - USE_ELASTICSEARCH=true
    volumes:
      - .:/app
      - uploads_data:/app/uploads
    depends_on:
      redis:
        condition: service_healthy
      weaviate:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
    restart: unless-stopped
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

  celery-worker:
    build:
      context: .
    platform: linux/amd64
    environment:
      - REDIS_URL=redis://redis:6379/0
      - WEAVIATE_URL=http://weaviate:8080
      - ELASTICSEARCH_URL=http://elasticsearch:9200
      - EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
      - RERANKER_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2
      - UPLOAD_DIR=/app/uploads
      - CHUNKING_STRATEGY=fixed
      - EXTRACTION_BACKEND=pymupdf
      - USE_ELASTICSEARCH=true
    volumes:
      - .:/app
      - uploads_data:/app/uploads
    depends_on:
      redis:
        condition: service_healthy
      weaviate:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
    restart: unless-stopped
    # concurrency is capped: each fork worker loads docling + the embedding model
    # (~1.5-2 GB each), and the default (one per CPU) exhausts the Docker VM memory
    command: celery -A worker.celery_app worker --loglevel=info --concurrency=2 --max-tasks-per-child=20

volumes:
  weaviate_data:
  redis_data:
  uploads_data:
  elasticsearch_data:

